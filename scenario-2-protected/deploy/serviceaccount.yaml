apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-service
  namespace: payment-demo
  labels:
    app: payment-service
  annotations:
    # SPIFFE ID for this service account will be:
    # spiffe://cluster.local/ns/payment-demo/sa/payment-service
    spire.openshift.io/spiffe-id: "true"
